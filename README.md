## Pre-requisites

* Java
* Maven
* Docker

## About

Run commands to build the app with maven.

* `mvn clean install`
* `docker network create love-network`
* `docker-compose build`
* `docker-compose up -d`
## Testing data

## Commands

