spring.datasource.url=jdbc:postgresql://localhost:5432/batchjobs
spring.datasource.username=postgres
spring.datasource.password=docker

spring.datasource.initialization-mode=always
spring.datasource.initialize=true
spring.datasource.continue-on-error=true

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.show-sql=true

spring.batch.initialize-schema=always
spring.batch.job.enabled=false

spring.flyway.baseline-on-migrate = true

#BATCH
batch.max-jobs=10
batch.chunk-size=1000
#Must be defined if xml import is needed
batch.root-name=root

#FLYWAY
#flyway.user=postgres
#flyway.password=docker
#flyway.schemas=batchjobs
#flyway.url=jdbc:h2:mem:batchjobs
#flyway.locations=/Users/dan/Workspace/java/batch-jobs/src/main/resources/db.migration
